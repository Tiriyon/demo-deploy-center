apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-configmap
data:
  test: |
    {{- range .Values.clusters }}
    {{- if eq .Values.wutcluster .name }}
    {{ .datapoint }}
    {{- end }}
    {{- end }}
