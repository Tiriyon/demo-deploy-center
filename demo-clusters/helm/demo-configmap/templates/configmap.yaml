apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-configmap
data:
  test: |
    {{- range .Values.clusters }}
    {{- if eq .name "wutcluster" }}
    {{ .datapoint }}
    {{- end }}
    {{- end }}
